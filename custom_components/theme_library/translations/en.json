{
  "config": {
    "step": {
      "user": {
        "title": "Set up Light Theme Library",
        "description": "This adds the Light Theme Library panel to your sidebar and a device with a button for each theme you favorite. Everything runs inside Home Assistant — no separate add-on needed."
      }
    },
    "abort": {
      "already_configured": "Light Theme Library is already set up."
    }
  }
}
